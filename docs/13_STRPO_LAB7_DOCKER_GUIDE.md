# 13. СТРПО Лаба 7 — Docker

## Что требует задание

- установить Docker;
- показать `docker --version`;
- показать `docker run hello-world`;
- собрать свой образ через Dockerfile;
- объяснить, что делают команды в Dockerfile;
- уметь различать image и container.

## Что уже подготовлено

Папка:
- `docker/`

В ней:
- `Dockerfile`
- `ascii_cat.cpp`
- `README.md`

## Команды

```bash
docker build -t ds_strpo_cat docker/
docker run --rm ds_strpo_cat
docker run --rm ds_strpo_cat "meow from container"
```

## Что говорить на защите

- **image** — это шаблон / слепок окружения;
- **container** — это конкретный запущенный экземпляр образа;
- `FROM` задаёт базовый образ;
- `RUN` выполняется на этапе сборки образа;
- `COPY` кладёт файлы внутрь образа;
- `CMD` задаёт команду по умолчанию при запуске контейнера.

## Важная честная ремарка

Docker внутри этого контейнера не запускался, потому что здесь его нет. Подготовлена сама лаба и все команды для запуска у тебя локально.
